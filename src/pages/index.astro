---
import Layout from '../layouts/base-layout.astro'
import Nav from '../components/nav.astro'
import Footer from '../components/footer.astro'
import BigContent from '../components/big-content.astro'
import BoxedText from '../components/boxed-text.astro'
import Logo from '../../public/mask-icon.svg'

const cx = {
  main: 'max-w-5xl mx-auto',
  content: 'font-black italic text-5xl px-4 py-32 max-w-lg text-center mx-auto leading-5 hyphens-auto',
  logo: 'align-middle stroke-current trace'
}

const theme = {
  light: '#ff72c6',
  dark: '#3a0023'
}
---
<Layout className={cx.main} {...theme}>
  <Nav />
  <BigContent class={cx.content}>
    <Logo class={cx.logo} />
    <BoxedText {...theme}>
      apps for us
    </BoxedText>
  </BigContent>
  <Footer />
</Layout>

